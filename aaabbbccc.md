# 项目用途说明

这个仓库是一个基于 Qt/C++ 的桌面应用项目，工程文件是 `fanqian.pro`，主要面向声纳浮标、潜艇目标、飞机、鱼雷等仿真/监控场景的数据接收、处理和显示。

## 这个项目是干什么用的

从项目文件和代码结构看，它主要用于搭建一个可视化的声纳/反潜相关数据监控与仿真界面：

1. **接收 DDS 数据**
   - `DDSManager` 负责注册和接收多个 DDS Topic。
   - Topic 覆盖声纳状态、声纳检测结果、目标运动状态、飞机状态、鱼雷状态、主动声纳、仿真步进等数据。

2. **展示数据接收情况**
   - `DataReceivingWidget` 用于显示 DDS 接收统计、最新 Topic、解析成功/失败数量，以及每条数据的摘要和详情。

3. **展示数据处理过程**
   - `DataProcessingWidget` 用于展示接收到的数据如何被处理，包括处理总览、处理过程、输出结果和定位相关信息。

4. **显示声纳浮标和目标信息**
   - `DisplayControlWidget` 提供多个页面，例如浮标状态、目标识别、主动声纳、定位结果等。
   - 可以展示浮标位置、浮标工作状态、声纳检测结果、目标置信度、目标经纬度、预测位置和定位误差等。

5. **地图可视化**
   - 工程配置中引用了 QGIS，代码里也使用 `QgisManager`。
   - 说明该项目会把浮标、目标、飞机、鱼雷等对象显示在地图上，用于直观看到位置和运动状态。

6. **依赖外部库和平台能力**
   - `fanqian.pro` 中配置了 Qt Widgets、Qt Multimedia、Qt XML、Qt SVG、Qt Location、Qt Positioning、Qt Network。
   - 还依赖 OpenCV、QGIS 3.16 和 DDSManager 相关库。

## 简单总结

这个项目不是普通网站或命令行工具，而是一个 Qt 桌面端仿真/监控系统。它的核心用途是：接收 DDS 发布的声纳、浮标、目标、飞机、鱼雷等仿真数据，进行解析和处理，并通过表格、状态页和地图界面展示出来，辅助观察反潜/声纳探测相关场景中的数据流、目标定位和态势变化。
